Django==4.1.2
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
gunicorn>=21.2.0
whitenoise>=6.6.0
markdown>=3.5.0
transformers>=4.30.0
Pillow>=11.0.0
# Note: torch is installed separately in Dockerfile using CPU-only build (much faster/smaller)

